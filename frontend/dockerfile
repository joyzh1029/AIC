# 1. 베이스 이미지 (Node.js 18-alpine 경량 이미지)
FROM node:18-alpine

# 2. 작업 디렉토리 설정
WORKDIR /app

# 3. 의존성 설치를 위한 package.json, package-lock.json 또는 yarn.lock 복사
COPY package*.json ./

# 4. 의존성 설치 (npm 사용 시)
RUN npm install

# 5. 프로젝트 소스 전체 복사
COPY . .

# 6. Vite dev 서버가 사용하는 포트 노출 (기본 5173이나, 변경8080)
EXPOSE 8080

# 7. 개발 서버 실행 명령
CMD ["npm", "run", "dev"]
